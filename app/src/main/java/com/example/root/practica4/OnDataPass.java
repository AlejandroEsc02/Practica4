package com.example.root.practica4;

public interface OnDataPass {
    void agrega (String nombre, int id, int cantidad, double precio);
    void busca (String nombre);
    void elimina (String nombre);
}
